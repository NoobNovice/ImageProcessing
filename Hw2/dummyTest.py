import cmath

x = 2.95
y = 20.3

print(int(x))
print(int(y))


 # print("Invert Fourier")
    # F_invert = np.matrix(F).T
    # g = invert_fourier(G,F_invert)
    # save2pgm(g,dir_path + "/Image/re_img.pgm")
    # image = cv2.imread(dir_path + "/Image/re_img.pgm",0)
    # cv2.imshow('image',image)
    # cv2.waitKey(0)